# Astro + Sanity CMS Landing Page

Проект лендінгу, побудований на базі **Astro** з інтеграцією **Sanity CMS** для управління контентом. Використовує React компоненти, Tailwind CSS та shadcn/ui для UI елементів.

## 📁 Структура проекту

### Коренева папка `verdant-venus/`

Основна папка проекту, що містить Astro додаток.

#### Конфігураційні файли

- **`astro.config.mjs`** - головна конфігурація Astro проекту. Налаштовує інтеграції (React, Tailwind, Sanity), підключає Sanity Studio на `/studio` роут
- **`package.json`** - залежності та скрипти для Astro проекту
- **`tsconfig.json`** - конфігурація TypeScript
- **`components.json`** - конфігурація shadcn/ui компонентів

#### Папка `src/`

- **`src/pages/index.astro`** - головна сторінка лендінгу. Отримує дані з Sanity CMS та рендерить всі блоки сторінки
- **`src/components/`** - React компоненти для різних секцій лендінгу:
  - `hero-block.tsx` - головний hero блок
  - `navbar3.tsx` - навігаційне меню
  - `footer3.tsx` - футер
  - `benefits-block.tsx` - блок переваг
  - `faq-block.tsx` - блок з питаннями та відповідями
  - `testimonials-block.tsx` - блок відгуків
  - `features-*.tsx` - різні блоки з функціями
  - `ui/` - базові UI компоненти (shadcn/ui): кнопки, картки, акордеони, тощо
- **`src/lib/`** - утиліти та допоміжні файли:
  - `sanityClient.ts` - клієнт для роботи з Sanity API
  - `fontawesome-icons.tsx` - конфігурація Font Awesome іконок
  - `utils.ts` - допоміжні функції
- **`src/styles/global.css`** - глобальні стилі проекту

#### Папка `public/`

Статичні файли (SVG іконки, логотипи, фонові зображення):
- `favicon.svg`
- `corefy_main_logo.svg`
- `corefy_main_mark.svg`
- `dots-bg.svg`
- `frame-*.svg`

### Папка `verdant-venus/test/`

Папка з конфігурацією та схемами **Sanity CMS**.

#### Конфігураційні файли

- **`sanity.config.ts`** - головна конфігурація Sanity Studio. Налаштовує проект ID, dataset, плагіни (Structure Tool, Vision Tool)
- **`sanity.cli.ts`** - конфігурація CLI для Sanity
- **`package.json`** - залежності та скрипти для Sanity Studio
- **`tsconfig.json`** - конфігурація TypeScript для Sanity

#### Папка `schemaTypes/`

Схеми даних для Sanity CMS:
- **`index.ts`** - експортує всі типи схем
- **`landingPage.ts`** - схема для лендінгу з полями для Hero секції, Benefits блоку та інших секцій

## 🚀 Як запустити проект

### Передумови

- Node.js (версія 18 або вище)
- npm або yarn

### Запуск Astro сайту

1. Перейдіть в папку проекту:
```bash
cd verdant-venus
```

2. Встановіть залежності:
```bash
npm install
```

3. Запустіть dev сервер:
```bash
npm run dev
```

Сайт буде доступний за адресою: `http://localhost:4321`

4. Для продакшн білду:
```bash
npm run build
```

5. Для перегляду продакшн білду:
```bash
npm run preview
```

### Запуск Sanity CMS адмінки

1. Перейдіть в папку з Sanity конфігурацією:
```bash
cd verdant-venus/test
```

2. Встановіть залежності:
```bash
npm install
```

3. Запустіть Sanity Studio:
```bash
npm run dev
```

Або використайте:
```bash
npm start
```

Sanity Studio буде доступна за адресою: `http://localhost:3333`

4. Для деплою Sanity Studio:
```bash
npm run deploy
```

### Альтернативний спосіб запуску Sanity Studio

Sanity Studio також інтегрована в Astro проект і доступна за адресою `/studio` після запуску Astro dev сервера:

1. Запустіть Astro:
```bash
cd verdant-venus
npm run dev
```

2. Відкрийте в браузері: `http://localhost:4321/studio`

## 🔧 Змінні оточення

Проект використовує наступні змінні оточення (опційно):

- `PUBLIC_SANITY_PROJECT_ID` - ID Sanity проекту (за замовчуванням: `l7n0o2d0`)
- `PUBLIC_SANITY_DATASET` - назва dataset в Sanity (за замовчуванням: `production`)
- `SANITY_API_READ_TOKEN` - токен для доступу до Sanity API (для preview режиму)

Створіть файл `.env` в корені `verdant-venus/` для налаштування:

```env
PUBLIC_SANITY_PROJECT_ID=your_project_id
PUBLIC_SANITY_DATASET=production
SANITY_API_READ_TOKEN=your_token
```

## 📦 Технології

- **Astro** - фреймворк для статичних сайтів
- **React** - для інтерактивних компонентів
- **Sanity CMS** - headless CMS для управління контентом
- **Tailwind CSS** - утилітарний CSS фреймворк
- **shadcn/ui** - набір UI компонентів
- **TypeScript** - типізація коду
- **Font Awesome** - іконки

## 📝 Примітки

- Sanity Studio налаштована на проект ID: `l7n0o2d0` та dataset: `production`
- Компоненти використовують `client:load` директиву для гідратації на клієнті
- Дані з Sanity отримуються на сервері під час білду (SSG)

